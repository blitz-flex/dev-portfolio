import React from 'react';

const Background = () => {
    return (
        <div className="fixed inset-0 z-0 pointer-events-none bg-bg-light dark:bg-bg-dark transition-colors duration-1000" />
    );
};

export default Background;
